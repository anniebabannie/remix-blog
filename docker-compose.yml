# Docker compose versions, not app versions
version: "3.8"
services:
  app:
    stdin_open: true
    tty: true
    build:
      context: ./
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
      - '3001:3001'
    volumes:
      - ${APP_DIR}:/app
      - /app/node_modules